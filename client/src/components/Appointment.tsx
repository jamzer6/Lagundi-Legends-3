// src/components/Dashboard.tsx
import React from 'react';

const Appointment: React.FC = () => {
  return <h1 style={{position: 'absolute', top: '500%'}}>Welcome to the Appointment!</h1>;
};

export default Appointment;
